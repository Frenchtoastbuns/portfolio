Place the current CV PDF in this folder as `Aaron_B_George_CV.pdf`.

Vite serves files in `public/` from the site root, so the portfolio button points to:

```text
/Aaron_B_George_CV.pdf
```

The circuit-board background is served as `/circuit-board-background.png`.

Supporting technical PDFs are stored in `public/documents/` and are linked from the project modals and publications section.
